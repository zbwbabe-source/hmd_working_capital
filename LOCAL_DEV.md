## Local Dev Rules

- Run the dev server from `d:\Cursor_work_space\Working Capital Dashboard\cashflow`
- Use `npm run dev`
- Do not run Next.js from the parent `Working Capital Dashboard` folder
- If styles are missing locally, stop the dev server, delete `.next`, and restart `npm run dev`
